# series
This project predicts future time-series values using Python. It prepares data, trains a model, and generates forecasts. The script handles preprocessing, splitting data, training, and plotting actual vs predicted values for easy understanding
